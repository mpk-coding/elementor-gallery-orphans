FN that takes the last orphan item from Elementor Gallery and fills the empty cells with it.

Boldly assuming it works most of the time.

HOW TO:
  Insert into custom html widget within <script> tags.
  You should probably call that fn in window.onload = () { ... };
